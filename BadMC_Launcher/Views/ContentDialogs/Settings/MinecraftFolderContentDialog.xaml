<ContentDialog x:Class="BadMC_Launcher.Views.ContentDialogs.Settings.MinecraftFolderContentDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:local="using:BadMC_Launcher.Views.ContentDialogs.Settings"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               x:Uid="MinecraftFolderContentDialog_ViewContentDialog"
               PrimaryButtonStyle="{StaticResource AccentButtonStyle}"
               Style="{ThemeResource DefaultContentDialogStyle}">
    <Grid MinWidth="400" MinHeight="350">
        <Grid.RowDefinitions>
            <RowDefinition Height="42" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ToolBar -->
        <Button Grid.Row="0" Height="36"
                    Padding="0"
                    HorizontalAlignment="Right">
            <StackPanel Orientation="Horizontal" Spacing="8" Margin="8,0">
                <FontIcon Glyph="&#xE712;" />
                <TextBlock x:Uid="MinecraftFolderContentDialog_AddItemMenuButton" />
            </StackPanel>
        </Button>

        <!-- Items -->
        <ListView Grid.Row="1" ItemTemplate="{StaticResource MinecraftFolderItemsTemplatePrimary}" ItemsSource="{Binding MinecraftFoldersList}">
            
        </ListView>
    </Grid>
</ContentDialog>

<UserControl x:Class="BadMC_Launcher.Views.UserControls.LoadingAnimation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:animations="using:CommunityToolkit.WinUI.Animations"
             xmlns:converters="using:CommunityToolkit.WinUI.Converters"
             xmlns:local="using:BadMC_Launcher.Views.UserControls"
             IsHitTestVisible="{x:Bind IsLoading}"
             Opacity="{x:Bind IsLoading, Converter={StaticResource BoolToOpacityConverter}}">
    <UserControl.Resources>
        <converters:BoolToObjectConverter x:Key="BoolToOpacityConverter"
                                          FalseValue="0"
                                          TrueValue="1" />
    </UserControl.Resources>

    <animations:Explicit.Animations>
        <animations:AnimationSet x:Name="LoadBeginAnimation">
            <animations:OpacityAnimation EasingMode="EaseOut"
                                         From="0"
                                         To="1"
                                         Duration="0:0:0.6" />
            <animations:TranslationAnimation EasingMode="EaseOut"
                                             From="0,100,0"
                                             To="0"
                                             Duration="0:0:0.6" />
        </animations:AnimationSet>

        <animations:AnimationSet x:Name="LoadEndAnimation">
            <animations:OpacityAnimation EasingMode="EaseIn"
                                         From="1"
                                         To="0"
                                         Duration="0:0:0.2" />
            <animations:TranslationAnimation EasingMode="EaseIn"
                                             From="0"
                                             To="0,100,0"
                                             Duration="0:0:0.2" />
        </animations:AnimationSet>
    </animations:Explicit.Animations>

    <StackPanel Padding="18"
                Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Spacing="8">
        <!-- Animation -->

        <ProgressRing Width="64"
                      Height="64"
                      Margin="8"
                      IsActive="True" />

        <TextBlock x:Name="LoadLoadDescriptionTextBlock"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Style="{ThemeResource BodyStrongTextBlockStyle}" />
    </StackPanel>
</UserControl>
